{
  "name": "inovative-studios",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build",
       "start": "node backend/server.js"
  }
}
